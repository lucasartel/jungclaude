# JungAgent instance identity
INSTANCE_NAME=JungAgent
AGENT_INSTANCE=jung_v1
INSTANCE_TIMEZONE=America/Sao_Paulo

# Central admin for this single installation.
# For new Telegram-only instances, prefer ADMIN_PLATFORM_ID and leave
# ADMIN_USER_ID empty so it is derived as sha256(ADMIN_PLATFORM_ID)[:16].
# Only set ADMIN_USER_ID directly when migrating an existing instance.
ADMIN_USER_ID=
ADMIN_PLATFORM=telegram
ADMIN_PLATFORM_ID=
TELEGRAM_ADMIN_IDS=

# Runtime behavior
PROACTIVE_ENABLED=true
ACTIVE_CONSCIOUSNESS_ENABLED=true
# Future IV.2 canary: keep disabled until local regression and production ISM probes are green.
ISM_PROMPT_CONTEXT_ENABLED=false
ISM_PROMPT_CONTEXT_ADMIN_ONLY=true
ENABLE_UNSAFE_ADMIN_ENDPOINTS=false
WORK_AUTONOMY_ENABLED=true
WORK_MAX_AUTONOMOUS_ACTIONS_PER_DAY=3
WORK_MAX_PENDING_TICKETS=3
WORK_NOTIFY_ADMIN_ON_TICKETS=true

# Optional deep web reading for saber overflow and Work research.
# Keep disabled until FIRECRAWL_API_KEY is configured.
FIRECRAWL_ENABLED=false
FIRECRAWL_API_KEY=
FIRECRAWL_MAX_PAGES_PER_RELEASE=3
FIRECRAWL_TIMEOUT_SECONDS=30
FIRECRAWL_STORE_RAW_CONTENT=false
FIRECRAWL_MAX_MARKDOWN_CHARS=6000

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_AUDIO_INPUT_ENABLED=true
TELEGRAM_AUDIO_TRANSCRIPTION_MODEL=openai/gpt-4o-transcribe
TELEGRAM_AUDIO_LANGUAGE=pt
TELEGRAM_AUDIO_MAX_BYTES=20000000
TELEGRAM_AUDIO_TRANSCRIPTION_TIMEOUT_SECONDS=45
TELEGRAM_AUDIO_REPLY_ENABLED=true
TELEGRAM_AUDIO_REPLY_MODEL=openai/gpt-4o-mini-tts-2025-12-15
TELEGRAM_AUDIO_REPLY_VOICE=nova
TELEGRAM_AUDIO_REPLY_FORMAT=mp3
TELEGRAM_AUDIO_REPLY_MAX_CHARS=1800
TELEGRAM_AUDIO_REPLY_TIMEOUT_SECONDS=45

# LLM providers
OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
CONVERSATION_MODEL=z-ai/glm-5
INTERNAL_MODEL=z-ai/glm-5

# Dream image generation.
# OpenRouter returns data URLs for Nano Banana 2; Pollinations is used as fallback.
IMAGE_GENERATION_ENABLED=true
DREAM_TEXT_FALLBACK_MODEL=openai/gpt-4o-mini
DREAM_IMAGE_PROVIDER=openrouter_nano_banana
DREAM_IMAGE_MODEL=google/gemini-3.1-flash-image-preview
DREAM_IMAGE_STYLE=impressionismo
DREAM_IMAGE_STYLE_PROMPT=pintura impressionista, pinceladas visiveis, cor luminosa, atmosfera vibrante, luz natural difusa, bordas suaves, textura pictorica, composicao poetica, sensacao de instante vivido, sem fotorrealismo, sem render 3D, sem anime, sem comic book, sem arte vetorial
HOBBY_ART_IMAGE_PROVIDER=openrouter_nano_banana
HOBBY_ART_IMAGE_MODEL=google/gemini-3.1-flash-image-preview

# Semantic memory via mem0 + Qdrant.
# Recommended for production installs. Use one collection per JungAgent instance.
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION_NAME=jung_memories_jung_v1
MEM0_LLM_MODEL=openai/gpt-4o-mini
MEM0_LLM_BASE_URL=https://openrouter.ai/api/v1
OPENAI_EMBEDDING_MODEL=openai/text-embedding-3-small
OPENAI_EMBEDDING_BASE_URL=https://openrouter.ai/api/v1

# Persistence
SQLITE_DB_PATH=
DATABASE_URL=

# Legacy/local semantic memory fallback.
# Production installs should prefer Qdrant above.
# Leave disabled in production unless you are intentionally running an old/local setup.
ENABLE_LEGACY_CHROMA=false
CHROMA_DB_PATH=
