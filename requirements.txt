# Telegram Bot (v21+ suporta httpx >= 0.27)
python-telegram-bot>=21.0

# OpenAI (versao compativel com numpy<2.0)
openai>=1.0.0,<1.68.0

# Environment
python-dotenv==1.0.0

# ChromaDB + LangChain (versoes compativeis)
chromadb==0.5.23
langchain>=0.1.0,<0.3.0
sentence-transformers>=2.2.2
langchain-chroma>=0.1.0,<0.2.0
langchain-community>=0.0.20,<0.3.0

# Pydantic
pydantic>=2.5.2,<3.0.0

# Numpy (compatibilidade com ChromaDB)
numpy>=1.26.0,<2.0.0

# FastAPI + Web
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Anthropic (para Claude)
anthropic>=0.40.0

# PDF Generation
reportlab>=4.0.0

# Security (bcrypt para autenticação)
bcrypt>=4.0.0

# BM25 (busca híbrida sobre logs de sessão)
rank-bm25==0.2.2

# mem0 (backend de memória persistente — substituição de ChromaDB + user_facts_v2)
mem0ai>=0.1.0
qdrant-client>=1.7.0
